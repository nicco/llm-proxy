services:
  llm-proxy:
    build: .
    container_name: llm-proxy
    ports:
      - "27878:7878"
    volumes:
      - ${HOME}/.llm-proxy:/root/.llm-proxy
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
